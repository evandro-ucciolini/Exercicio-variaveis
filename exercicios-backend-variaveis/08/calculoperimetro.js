

const raio = 1
const comprimento = (2 * Math.PI * 1).toFixed(1)
const area = (Math.PI * raio ** 2).toFixed(2)



console.log(`A partir do raio ${raio} de um circulo, o tamanho aproximado do comprimento é de ${comprimento} e o aproximado da área é de ${area}.`)

